local item = payload[0];
{
    taskId : item.task_id,
    userId : item.user_id,
    done : item.done,
    name : item.name,
    deadline : item.deadline,
    progress : item.progress,
    priority : item.priority
}